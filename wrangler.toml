name = "rippner-scheduling"
compatibility_date = "2025-01-01"
pages_build_output_dir = "public"

# D1 binding is configured in the Cloudflare Pages dashboard
# (Settings → Bindings → D1 database → variable name: DB)
